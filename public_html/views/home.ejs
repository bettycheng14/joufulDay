<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Melbourne Tours - JoyfulDay</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="css/style.css" rel="stylesheet">
</head>

<body>
    <%- include('partials/header') %>
        <div id="heroCarousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-indicators">
                <button type="button" data-bs-target="#heroCarousel" data-bs-slide-to="0" class="active"></button>
                <button type="button" data-bs-target="#heroCarousel" data-bs-slide-to="1"></button>
                <button type="button" data-bs-target="#heroCarousel" data-bs-slide-to="2"></button>
            </div>
            <div class="carousel-inner">
                <!-- Slide 1 -->
                <div class="carousel-item active">
                    <img src="images/home.jpg" class="d-block w-100" alt="Melbourne Skyline">
                    <div class="carousel-caption d-none d-md-block">
                        <h2>Explore Melbourne</h2>
                        <p>Discover the city's vibrant culture and iconic landmarks</p>
                    </div>
                </div>
                <!-- Slide 2 -->
                <div class="carousel-item">
                    <img src="images/great_ocean_road.avif" class="d-block w-100" alt="Great Ocean Road">
                    <div class="carousel-caption d-none d-md-block">
                        <h2>Great Ocean Road Adventures</h2>
                        <p>Experience breathtaking coastal views and natural wonders</p>
                    </div>
                </div>
                <!-- Slide 3 -->
                <div class="carousel-item">
                    <img src="images/phillip_island.avif" class="d-block w-100" alt="Phillip Island">
                    <div class="carousel-caption d-none d-md-block">
                        <h2>Phillip Island Excursions</h2>
                        <p>Witness the famous Penguin Parade and wildlife encounters</p>
                    </div>
                </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
                <span class="carousel-control-next-icon"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
        <!-- Quick Search Bar -->
        <div class="container my-4">
            <h3 class="mb-2 fw-bold">Explore for more</h3>
            <form class="row g-3" method="GET" action="/destinations">
                <div class="col-md-5">
                    <input type="text" class="form-control" placeholder="Search by tour name or area" name="q" value="">
                </div>
                <div class="col-md-5">
                    <select class="form-select" name="category">
                        <option value="">All Tour Types</option>
                        <% categories.forEach(cat=> { %>
                            <option value="<%= cat %>">
                                <%= cat.replace(/([a-z])([A-Z])/g, '$1 $2' ).replace(/^./, str=> str.toUpperCase()) %>
                            </option>
                            <% }) %>
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary w-100">Search</button>
                </div>
            </form>
        </div>

        <!-- Top 6 Tours Grid -->
        <section class="py-4">
            <div class="container">
                <h3 class="mb-4 fw-bold">Top 6 Tours</h3>
                <div class="row row-cols-1 row-cols-md-3 g-4">
                    <% tours .sort((a, b)=> b.rating - a.rating)
                        .slice(0, 6)
                        .forEach(tour => { %>
                        <div class="col">
                            <div class="card h-100 tour-card position-relative">
                                <% if(tour.isPopular){ %>
                                    <span class="badge-popular">Popular</span>
                                    <% } %>
                                        <img src="<%= tour.imageUrl %>" class="card-img-top" alt="<%= tour.title %>">
                                        <div class="card-body d-flex flex-column">
                                            <h5 class="card-title">
                                                <%= tour.title %>
                                            </h5>
                                            <p class="card-text">
                                                <%= tour.description.substring(0, 100) %>...
                                            </p>
                                            <p class="fw-bold">A$<%= tour.price %> / person</p>
                                            <div class="mt-auto align-self-end">
                                                <a href="/tour/<%= tour._id %>" class="btn btn-primary">View Details</a>
                                            </div>
                                        </div>
                            </div>
                        </div>
                        <% }) %>
                </div>
            </div>
        </section>
        <%- include('partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>